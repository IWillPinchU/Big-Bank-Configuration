server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:Add commentMore actions
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
